# 业主端 UI 评审意见与整改清单

## 1. 评审基准

本轮评审以以下两张图为唯一视觉与体验对标基准：

- `C:/Users/14894/Downloads/ChatGPT Image 2026年5月5日 22_43_32 (1).png`
- `C:/Users/14894/Downloads/ChatGPT Image 2026年5月5日 22_43_33 (2).png`

目标不是“把功能摆全”，而是把既有业务闭环组织成一个真实可用、可点、可理解的移动端 App 首页与多页体验。

---

## 2. 当前实现的核心问题

### P0. 首页被切成多个汇报式版块，不像一个连续的移动端任务首页

当前 `resident.html` 首页由多个 `resident-section` 直接顺序堆叠组成：

- 当前提醒
- 推荐方案
- 其他处理方式
- 帮你判断
- 处理进度
- 当前奖励

对应代码位置：

- `resident.html:41-106`

这会直接导致首页阅读体验像“报告分段”，而不是效果图里的“单一长画布连续下滑”。  
效果图首页的正确组织方式是：

1. 用户问候与场景图
2. 地址胶囊
3. 当前问题英雄卡
4. 为什么优先处理
5. 优选解决方式 Banner
6. 其他解决方式
7. 决策辅助 Banner
8. 底部导航

这些内容虽然逻辑上不同，但视觉上必须被组织在同一个连续画布里，通过留白、排版、轻标题、插画 Banner 来转场，而不是每一段都再起一个标准 section 标题盒子。

### P0. 首页标题和首屏叙事错了，没有把“当前这一件事”放在最前面

当前首页顶部是通用产品标题区：

- `畅楼生活`
- `先把这件事处理好`
- 住户切换器

对应代码位置：

- `resident.html:21-37`
- `resident.js:308-315`

这里的问题是：

1. 首屏没有使用效果图中的“头像 + 问候语 + 一句暖提示 + 场景插画 + 消息铃铛”的个人化入口。
2. `resident.js` 在首页强行把标题改成了 `楼道提醒`，这会让首页更像通知页，而不是 C 端任务处理首页。
3. 住户切换器直接裸露在头部，像测试控件，不像 App 内容。

### P0. 样式 token 还是旧后台式冷色系统，和新设计规范不一致

当前根样式变量仍以冷灰、蓝、青为主：

- `styles.css:1-23`

例如：

- `--bg: #eef2f6`
- `--text: #17212b`
- `--navy: #0f172a`
- `--teal: #0f766e`

但设计规范图明确给了新的暖色体系：

- 主色暖橙 `#FF8A3D`
- 辅助杏米色 `#FFE7C8`
- 奶油白 `#FFF9F3`
- 柔和绿色 `#6BA26B`
- 浅棕 `#D4B08A`
- 深棕文字 `#5A3E2B`
- 中性灰 `#8E96A3`
- 功能红 `#E15D4D`

这意味着现在不是“局部卡片调暖了”，而是主题层还没重构完。前端现在是在旧系统上补暖色块，所以整体气质依然不统一。

### P1. 太依赖文字标题分块，缺少效果图里的承载型组件

当前实现大量依赖：

- `resident-section-heading`
- `kicker`
- `h2`

对应代码位置：

- `resident.html:44-105`
- `resident.js:1926-1960`

而效果图里，真正承担叙事切换的不是“标题栏”，而是组件本身：

- 问题英雄卡承担“当前提醒”
- 理由芯片卡承担“为什么优先处理”
- 推荐方案 Banner 承担“最佳路径推荐”
- 决策辅助 Banner 承担“偏好输入”

也就是说，现状是“标题在讲话，组件在填空”；目标应该是“组件自己会讲话，标题只做很轻的辅助”。

### P1. 进度、资产、消息等页面虽然分了 tab，但页面内部仍偏静态汇报，不够像 App

当前实现已经有底部导航，但页面内部仍大量使用统一结构 section 卡片：

- `resident.html:109-205`

这会带来两个问题：

1. 页面层级虽然分开了，但每一页内部还是“模块清单”感。
2. 和效果图想要的“任务大厅 / 资产页 / 消息页 / 家人代办页”相比，缺少主次层级、反馈态和空状态叙事。

### P2. 底部导航图标和交互质感还不够产品化

当前底部导航是汉字缩写：

- `办 / 邻 / 信 / 家 / 我`

对应代码位置：

- `resident.html:220-240`
- `styles.css:1766-1787`

这更像占位实现，不像正式移动端导航。效果图里底部导航应使用统一图标体系，选中态是“图标容器 + 标签 + 高亮块”，不是文字代替图标。

---

## 3. 必须补齐的 UI 组件

以下组件请直接按效果图补齐，不要继续用普通 section 标题代替。

### 3.1 首页顶部组件

1. **问候头部 Hero**
   - 左侧头像
   - 主标题：`李阿姨，您好`
   - 副标题：`整洁楼道，守护我们共同的家`
   - 右侧场景插画
   - 右上角消息铃铛
   - 红点未读提醒

2. **地址胶囊**
   - 左侧定位图标
   - 中间地址文案
   - 右侧箭头

### 3.2 当前问题区组件

3. **问题英雄卡**
   - 标题：`楼道杂物问题`
   - 紧急标签：`需尽快处理`
   - 一句简洁问题说明
   - 右侧盾牌警示插画

4. **四宫格能力小卡**
   - 24 小时内响应处理
   - 智能方案推荐
   - 共建整洁楼道 / 邻里互助
   - 居民参与 / 共建家园

### 3.3 优先原因区组件

5. **原因卡容器**
   - 标题：`为什么要优先处理这个问题？`
   - 暖色轻图标引导

6. **三枚原因芯片**
   - 影响通行与消防，存在安全隐患
   - 居住体验下降，环境脏乱差
   - 邻里关系更和谐，共建美好家园

### 3.4 推荐方案区组件

7. **推荐方案 Banner 卡**
   - 标题：`优选解决方式：邻里互助清理`
   - 推荐标签
   - 一句短说明
   - 右侧人物插画

8. **推荐方案优势胶囊**
   - 邻里互助
   - 工具支持
   - 时间灵活
   - 环保处理

### 3.5 其他处理方式组件

9. **解决方式列表卡 x 3**
   - 预约清洁
   - 现场回收
   - 自行处理

每张卡必须包含：

- 图标
- 标题
- 一句副标题
- 主按钮

### 3.6 决策辅助组件

10. **决策辅助 Banner**
    - 标题：`做个简单选择，下一步更顺利`
    - 一句辅助说明
    - 右侧清单插画

11. **偏好选择胶囊**
    - 时间充裕
    - 希望省心
    - 想要省钱
    - 尽快解决

### 3.7 导航组件

12. **底部导航**
    - 首页
    - 邻里互助
    - 消息
    - 活动
    - 我的

要求：

- 使用统一图标
- 选中态有明显高亮容器
- 文案和图标为一个整体

---

## 4. 首页重排要求

首页必须改成一个连续长画布，不能再做成多个粗暴切开的 section 报告页。

### 正确结构

1. 顶部问候 Hero
2. 地址胶囊
3. 当前问题英雄卡
4. 英雄卡内的四个能力小卡
5. 优先处理原因卡
6. 推荐方案 Banner
7. 其他解决方式列表
8. 决策辅助 Banner
9. 底部导航

### 错误做法

- 每一段都单独套一个标准 `resident-section`
- 每一段都放一个 `kicker + h2 + 内容`
- 用大面积边框盒子强制切开
- 用“看提醒 / 去资产页”这种后台式尾部链接破坏首页节奏

### 正确分隔方式

只能用以下方式切换版块：

1. 留白节奏
2. 轻标题
3. Banner 卡
4. 插画卡
5. 信息胶囊组

不能再用“粗暴标题栏 + 白底卡片 + 下一块再来一遍”的重复模式。

---

## 5. 给前端的具体修改意见

### 5.1 结构层

1. 首页改成一个连续滚动容器，不再在首页内部强调多段 section 边界。
2. `status-card`、`current-event`、`recommended-option`、`options`、`home-decisions`、`progress`、`reward` 这些业务模块可以继续保留，但必须重组进一套连续首页叙事里。
3. 首页里不要再渲染统一的 section heading 体系，`renderHomeSectionHeadings()` 这一层应被弱化或移除。

### 5.2 样式层

1. 全量切换到暖色 design token，不要再混用旧后台蓝灰系统。
2. 背景应以奶油白、杏米色、轻暖渐变为主，不是冷灰大底。
3. 卡片要有“可触感”：
   - 大圆角
   - 环境阴影
   - 接触阴影
   - 顶部柔光
4. 文本颜色切换到深棕主文案 + 中性灰辅助文案。

### 5.3 组件层

1. 所有主动作卡必须采用“卡片即决策”的方式，不是纯说明文字。
2. 每个动作卡固定展示：
   - 适合谁
   - 预计耗时
   - 处理成本
   - 可获得奖励
   - 后续服务承接
3. 所有图标统一风格，不能继续用文字充当图标。

### 5.4 交互层

1. 首页优先推荐一个主方案，其他方案作为次级入口。
2. 用户点击主方案后，进入抽屉或二级页确认，而不是原地堆更多说明。
3. 进度页要像订单页：
   - 当前状态
   - 预计下一步
   - 谁在处理
   - 用户是否还要操作
4. 资产页要像账户页：
   - 总资产
   - 住户积分账户
   - 志愿贡献积分账户
   - 待到账
   - 已到账
   - 已使用
   - 已解锁
   - 最近到账
   - 徽章与等级进度

---

## 6. 本轮整改优先级

### P0 本轮必须完成

1. 首页改成长画布结构
2. 首页补齐效果图核心组件
3. 去掉汇报式分块感
4. 全量切换暖色 token
5. 底部导航换成正式图标体系

### P1 下一轮继续

1. 进度页做成更强的订单体验
2. 资产页做成更强的账户体验
3. 消息页补红点、空状态、到账态、成功态
4. 家人代办页补“有代办人 / 无代办人”双态

### P2 展示打磨

1. 插画资源进一步统一
2. 微动效补齐
3. 按钮按压态、到账反馈、报名成功态更细化

---

## 7. 验收标准

整改后的首页必须满足以下标准：

1. 一眼看上去是移动端 App，不是 H5 汇报页
2. 首屏一眼能看懂：
   - 发生了什么
   - 为什么先处理
   - 推荐怎么处理
3. 首页是一个连续长画布，不是多个粗暴分段模块
4. 组件风格、颜色、圆角、阴影与效果图气质一致
5. 所有业务内容仍保留，只是重新组织成更符合 C 端认知的界面
